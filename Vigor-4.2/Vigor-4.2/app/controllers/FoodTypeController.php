<?php
class FoodTypeController extends BaseController {

	/**
	 * Display a listing of the resource.
	 *
	 * @return Response
	 */
	public function index()
	{
		/*$categories = Food::lists('title', 'id');
		foreach (Food::all() as $cat)
		{
		$categories[] = array($cat->id => $cat->category);
		$id = $category['id'];
		$value = $category['category'];
		$cats[] = array($id => $value);
		}*/
	}
}
